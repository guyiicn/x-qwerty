# 更新记录

版本号对应 [Releases](https://github.com/guyiicn/x-qwerty/releases)。每条只记做了什么和**为什么**，踩坑的细节在 `NAVIGATION_PLAN.md` 里。

## 0.8.2

- 修滚动检测：上一版用**单个变量**存「上一个滚动目标」，页面上多个元素交替派发 scroll 时每次都会命中「目标变了就重置基准」的分支直接返回，永远算不出差值。改成按元素分别记位置
- 向上的判定改为累计位移 —— 单次 scroll 事件的位移常常只有几像素，单次阈值会漏判
- 抽屉底部加一行诊断：`版本 · 滚动事件数 · 滚动目标 · 位置 · 底栏状态`。前两次都是盲改盲发，有这行才能在真机上一眼定位问题在哪一环
- 显示版本号 —— 之前没法确认装的是哪个构建

## 0.8.1

- 修底栏滚动隐藏在真机上完全不工作：**X 的时间线是内部容器在滚，不是整页滚动**，原生 `WebView.setOnScrollChangeListener` 收不到任何事件。改用注入脚本里**捕获阶段**的 `document` scroll 监听
- 换滚动容器时重置基准，避免不同坐标系算出垃圾差值

## 0.8.0

- **底栏改为浮在正文上**，不再压缩视口。原来 78dp 是从页面可视高度里扣掉的，X 以为屏幕就那么高；现在视口完整，只在文档末尾补 `padding-bottom`（时间线无限，那段永远滚不到）
- 向下滚收起、向上滚或回到顶部再出现
- 沉浸模式：隐藏系统状态栏，再多 24dp。两个全开约多出 **102dp**
- 抽屉加两个开关（底栏自动隐藏/常驻、沉浸模式开/关），选择会记住
- 底栏底色改成完全不透明 —— 浮到正文上之后，半透明会把下面的文字透出来

## 0.7.0

- `Enter` 进入选中的帖子。只点 href 以 `/status/<数字>` **结尾**的链接，否则会点中 `/photo/1` 变成打开图片
- 列表页自动选中第一条，打开就能直接 `j`/`k`
- 选中项滚出视野后，`j`/`k` 从当前可见位置继续，不再跳回老位置
- 新增 `testing/test-timeline.html` 本地假信息流 —— 模拟器登录不了 X，这类逻辑没法在真实页面上验

## 0.6.2

- 快捷键清单按黑莓键盘的实际可用性重排。项目从一开始就只面向没有 Ctrl/Esc/F 键/方向键的键盘，但清单把一整屏按不出来的组合键排在最前面，开屏就是噪音
- 组合键沉到末尾，每条标明本机的等效按法

## 0.6.1

- 抽屉加「快捷键一览」全屏页。之前只写在 `README.md` 里，手机上根本看不到
- 除键盘外也列了触屏手势 —— 不写出来没人会发现

## 0.6.0

- `t` 真正完成转发，不再只是打开菜单。X 的转发图标点开是个菜单，要等菜单出现再点 `retweetConfirm`
- `q` 引用转发
- `u` 刷新当前视图（回顶 + 拉新帖，走 SPA 不重载整页），点已选中的 Tab 同效果
- 转发后提示「再按一次撤销」—— 转发本身可切换，比实现可点浮层简单也更可靠

## 0.5.1

- `g` 前缀层改走 SPA 路由，并和底栏/抽屉对齐。整理清单时才发现三套入口行为不一致：`g h` 还在整页跳、`g s` 和底栏去不同地址、`g p` 去的是设置页
- 新增 `g n` / `g k` / `g c`，前缀层覆盖抽屉全部条目

## 0.5.0

- 抽屉内置主题开关（跟随/浅色/深色）。原理是 X 把主题存在 `night_mode` cookie 里，壳写 cookie 再重载，外壳随后自动跟上页面配色 —— 壳和页面永远一致
- 隐藏时间线顶部的发推框（和原生 FAB 重复）。纯 CSS 选不中，改成从 `tweetTextarea_0` 向上找第一个含 `toolBar` 且不含 `article` 的祖先
- 抽屉条目区改为可滚动，工具行固定在底部

## 0.4.1

- 底栏加第 6 格「我」作为抽屉入口。**只给手势入口是错的** —— 手势导航的设备上左边缘属于系统返回，边缘右滑根本到不了 App，用户反馈「我就没见到抽屉」
- 隐藏 X 自己的浮动发推按钮

## 0.4.0

- 抽屉：个人资料/书签/列表/社区/已喜欢/草稿/设置，底部字号调节
- 自写 `EdgeSwipeLayout` 做左边缘拖拽，没有引入 AndroidX 的 DrawerLayout
- 修了一个一直存在的 bug：`applyPageTheme()` 开头的提前返回在初值恰好等于页面色时会跳过首次上色

## 0.3.2

- 修 0.3.0 引入的回归：进详情页底栏整条消失，回不了 TL。X 详情页顶部的**内联回复框**用的也是 `tweetTextarea_0`，旧代码在拿不到对话框时回退成 `document`，导致每个详情页都被当成「发帖框打开」。拆成两个独立信号

## 0.3.1

- 键盘层：`f` 前进、`z` 前缀层管字号、空格翻页、`g b`/`g l`、`Alt+Enter` 发布
- 字号走原生 `WebSettings.setTextZoom`，不在网页层做

## 0.3.0

- 底栏重做：5 个图标 Tab + 选中态 + 未读角标，发帖挪到 FAB
- 切 Tab 走 SPA 路由（点 X 自己的导航链接），保住滚动位置和时间线状态
- 图标用 `Path` + 自写 `IconDrawable` 画，零依赖不变

## 0.2.2

- SPA 路由桥：包 `pushState`/`replaceState` + `popstate`，回传 `routeChanged`
- 外壳配色跟随页面实际背景色

## 0.2.1

- 重新隐藏 X 的左侧导航栏。验证发现**返回箭头根本不在 `header[role="banner"]` 里** —— 原来点不到返回只有「被状态栏盖住」一个原因

## 0.2.0

- **修「返回按钮被遮住」**：`targetSdk 36` 在 Android 15+ 强制 edge-to-edge，而 inset 监听只算了 bottom，网页顶部画在状态栏底下。显式打开 edge-to-edge 让 API 24–36 走同一条路径
- **修返回键永远退不出 App**：没历史时又 `loadUrl(HOME)` 把历史种回来。改成双击退出
- 清掉 `x.com -> /home` 重定向留下的历史
- 发帖框的底部留白只作用于发帖对话框，不再影响图片查看器和菜单
